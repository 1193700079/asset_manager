1000 张大概 50 万 token 大概 4块钱成本  kimi k2.6 
10000 张大概 500 万 token



预筛选
需要分类， 完全不需要的去 skip
训练素材 （里面肯定是完整包含换脸素材的哈） 换脸素材是训练素材的子集，但是展示的时候可以勾选是否把这个换脸素材也统一展示也可以进行关闭
换脸素材 需要有这两个专属的 tab
同时每一个经过 ai 打标的话都需要有对应的理由哈，比如拒绝的理由通过的理由

人类反馈素材，比如从 skip 中回复的
从训练素材 or 换脸素材中恢复的

同时目前预筛选打标的时候 只有并发数 没有说这个可以同时批处理的图片数
比如并发数是 6，同时批处理的图片数也是 6（代表一个 api 请求带了 6 张图这样）


刚才只处理了图片的预筛选，这个批量的图片分析也完全可以说这样
比如并发数是 6，同时批处理的图片数也是 6（代表一个 api 请求带了 6 张图这样）

为什么现在 CM 系统只有一个了啊批处理,还有批处理换脸,批处理文生图，批处理图像编辑,批处理视频生成哈, 先说批处理换脸就是从数据库里面的换脸素材图片的 prompt 进行换脸（换脸的 base 就是角色的 头像）,同时也有zimage 生图,再用换脸,两个都要有（都是换脸）,要注明不同的模型来源,
文生图的话就是针对每个角色随机从素材库抽取 10 个 prompt 去进行生图，然后放在待选中，确定选中之后可以上到 profile image
图片编辑也是哈 针对每个角色随机从素材库抽取 10 个 prompt 去进行生图，图片的 base 是这个人物的头像
，批处理视频生成就是根据素材库中有 video prompt ，和对应 video prompt 的首帧来合成视频，会有换脸后的图，也有对应 edit好后的图 （注明来源是 edit 还是换脸）



这个 face